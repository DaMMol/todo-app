name: deploy backend
on:
  push:
    paths:
      - backend/**
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Render Deploy Hook
        run: |
          curl "${{ secrets.RENDER_BACKEND_DEPLOY_HOOK_URL }}"
